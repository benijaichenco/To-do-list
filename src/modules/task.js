export default class Task {
  constructor(title, description, date, project, completed) {
    this.title = title;
    this.description = description;
    this.date = date;
    this.project = project;
    this.completed = completed;
  }
}
